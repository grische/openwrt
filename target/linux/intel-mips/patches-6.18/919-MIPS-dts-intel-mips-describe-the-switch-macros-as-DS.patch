From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Grische <github@grische.xyz>
Date: Fri, 4 Sep 2026 15:30:38 +0200
Subject: MIPS: dts: intel-mips: describe the switch macros as DSA switches

The ethernet description on these boards was shaped by the driver suite
that read it. A virtual node with no registers, parented to nothing, held
one netdev per front-panel socket, and each of those named its datapath
port by an index the vendor allocator happened to hand out. The switch
macros had two sub-apertures described as devices of their own. The five
integrated PHYs were one list of identifiers with a list of reset lines
beside it, split across two files by die and overridden per board. The
queue manager's register windows and segment pools were fetched by their
position in the list, so reordering either silently mis-mapped every
entry, and its four DMA controllers took their identity from an alias and
their tuning from thirteen properties.

Describe the hardware instead. Each macro is a DSA switch: a port per
front-panel socket carrying its label, a CPU port pointing at the conduit
the die wires it to, the MDIO bus and the firmware loader as children with
no window of their own, and one child per integrated PHY holding the
offset of its firmware address register pair and the reset line the loader
holds it on. The two macros are two trees, because a conduit carries
exactly one CPU port and nothing forwards between the macros in hardware,
so a bridge spanning both would blackhole. The manager names its eleven
windows, its interrupts and its pools, and the two conduits are children
of it: a conduit owns no register of its own, and there is one per macro.
The DMA controllers keep a window and an interrupt, and nothing else,
because nothing else about them is board data.

The window the vendor device tree calls sbim is not carried over. Nothing
maps it, nothing reads it, and no register description for its address
exists in the vendor sources, so it cannot be named; the binding says the
same.

Front-panel LAN-N reaches switch port 6-N. The board wires the sockets in
the opposite order to the port numbering and these labels are the only
record of it. The fifth PHY, on the die variant that has one, carries the
socket on the second macro, whose port is numbered 15 while its PHY
answers at address 1.

Each PHY child names two reset cells, the request bit and the status bit,
and on this SoC those are different numbers. RCU_RST_STAT is a remap of
RCU_RST_REQ rather than a mirror of it, so a second cell that repeats the
first watches an unrelated bit and can only ever time out. The per-domain
map is spelled out at the reset controller node, and the five integrated
PHY lines take their status bits from it: request 29 reports in 28, 28 in
23, 26 in 22, 25 in 25, and the fifth core's request bit 31 reports in 30
- bit 31 of the status word being the watchdog reset cause, which has no
request counterpart at all.

Assisted-by: Claude:claude-opus-5
Signed-off-by: Grische <github@grische.xyz>

diff --git a/arch/mips/boot/dts/intel-mips/seale_avm.dtsi b/arch/mips/boot/dts/intel-mips/seale_avm.dtsi
index 3fd7be6ff80b..9ca59ba1ff2a 100644
--- a/arch/mips/boot/dts/intel-mips/seale_avm.dtsi
+++ b/arch/mips/boot/dts/intel-mips/seale_avm.dtsi
@@ -37,53 +37,71 @@ nand0: nand@0 {
 };
 
 /*
- * Both GSWIP-3.0 instances; on-die blocks with no board wiring.
- *
- * The GPHY firmware loader and the MDIO bus are not devices of their own:
- * their registers are sub-apertures of the switch register window this node
- * already claims. They are therefore reg-less children, and each driver
- * derives its window from this node's reg plus a fixed offset into the IP
- * block. The switch driver creates the platform devices for them.
+ * The LAN switch macro. The loader for the integrated PHYs and the MDIO bus
+ * are sub-apertures of the window this node already claims, so both are
+ * children with no window of their own and the switch driver reaches them
+ * through fixed offsets from it.
  */
 &gswl {
 	status = "okay";
 
 	/*
-	 * GPHY firmware loader, at switch base + 0x3c00. Its window must
-	 * cover the per-GPHY LBADR/MBADR pairs at 0x228..0x26f within it.
+	 * One child per integrated PHY. Its reg is the offset of the PHY's
+	 * firmware address register pair inside the switch window, and its
+	 * reset is the line the loader holds the core on while it writes that
+	 * pair. The reset bit list is not contiguous, so every entry is spelled
+	 * out, and the status bit each request bit reports through is a
+	 * different number again -- see the reset controller's own comment in
+	 * xrx500.dtsi for the map.
+	 *
+	 * A core without firmware never answers MDIO, so every core described
+	 * here is reset and pointed at the image this node names.
 	 */
 	gphy_fw: gphy-fw {
-		compatible = "lantiq,phy-xrx500";
+		compatible = "lantiq,xrx500-gphy-fw";
 		firmware-name = "lantiq/xrx500-phy-fw.bin";
-		status = "okay";
+		#address-cells = <1>;
+		#size-cells = <0>;
+
+		gphy@f8a {
+			reg = <0xf8a>;
+			resets = <&rcu_reset0 29 28>;
+			reset-names = "gphy";
+		};
+
+		gphy@f8e {
+			reg = <0xf8e>;
+			resets = <&rcu_reset0 28 23>;
+			reset-names = "gphy";
+		};
+
+		gphy@f92 {
+			reg = <0xf92>;
+			resets = <&rcu_reset0 26 22>;
+			reset-names = "gphy";
+		};
+
+		gphy@f96 {
+			reg = <0xf96>;
+			resets = <&rcu_reset0 25 25>;
+			reset-names = "gphy";
+		};
 	};
 
 	/*
-	 * MDIO bus, at switch base + (MDCTRL dword 0x004 + GSW30_TOP_OFFSET
-	 * 0xf00) * 4 = 0x3c10, a 0x20 span covering MDCTRL, MDREAD, MDWRITE
-	 * and the two MDCCFG registers.
+	 * The switch driver registers this bus itself, so it carries no
+	 * compatible and is not a device of its own.
 	 *
-	 * The bus defers probing until the GPHY firmware loader has run: a
-	 * PHY without firmware never answers MDIO.
+	 * A PHY's address on the bus is the number of the switch port it
+	 * serves. The identifier is spelled out on every node so the PHY
+	 * device is created from the device tree alone: these cores answer
+	 * their identification registers before the firmware the SoC loads has
+	 * finished booting, so a bus scan can sample a half-alive core.
 	 */
-	gswip_mdio: mdio {
-		compatible = "lantiq,gswip-mdio";
-		lantiq,gswip-instance = <0>;
+	mdio {
 		#address-cells = <1>;
 		#size-cells = <0>;
-		status = "okay";
 
-		/*
-		 * Each label is consumed by the port whose intel,dp-port-id
-		 * equals this PHY's MDIO address. Getting the pairing wrong
-		 * gives a working link on the wrong front-panel socket, which
-		 * no probe-time check can catch.
-		 *
-		 * The identifier is spelled out on every node so the PHY device
-		 * is created from the device tree alone. These cores answer
-		 * their ID registers before the firmware the SoC loads has
-		 * finished booting, so a bus scan can sample a half-alive core.
-		 */
 		gphy_lan1: ethernet-phy@5 {
 			compatible = "ethernet-phy-idd565.a409",
 				     "ethernet-phy-ieee802.3-c22";
@@ -108,55 +126,53 @@ gphy_lan4: ethernet-phy@2 {
 			reg = <0x02>;
 		};
 	};
-};
-
-&gswr {
-	status = "okay";
-};
 
-/ {
 	/*
-	 * Datapath netdev parent. Each child names one front-panel port and
-	 * its datapath port id; the driver allocates one netdev per child.
+	 * Front-panel LAN-N reaches switch port 6-N. The board wires the
+	 * sockets in the opposite order to the port numbering, and these
+	 * labels are the only thing that records it: getting them the wrong way
+	 * round gives four working ports with the wrong names on them, which no
+	 * probe-time check can catch.
 	 */
-	intel_xrx500_net: ethernet {
-		compatible = "intel,xrx500-net";
-		#address-cells = <1>;
-		#size-cells = <0>;
-		status = "okay";
-
-		port@0 {
-			reg = <0>;
-			intel,dp-port-id = <5>;
-			phy-handle = <&gphy_lan1>;
-		};
-
-		port@1 {
-			reg = <1>;
-			intel,dp-port-id = <4>;
-			phy-handle = <&gphy_lan2>;
-		};
-
+	ports {
 		port@2 {
 			reg = <2>;
-			intel,dp-port-id = <3>;
-			phy-handle = <&gphy_lan3>;
+			label = "lan4";
+			phy-mode = "internal";
+			phy-handle = <&gphy_lan4>;
 		};
 
 		port@3 {
 			reg = <3>;
-			intel,dp-port-id = <2>;
-			phy-handle = <&gphy_lan4>;
+			label = "lan3";
+			phy-mode = "internal";
+			phy-handle = <&gphy_lan3>;
+		};
+
+		port@4 {
+			reg = <4>;
+			label = "lan2";
+			phy-mode = "internal";
+			phy-handle = <&gphy_lan2>;
+		};
+
+		port@5 {
+			reg = <5>;
+			label = "lan1";
+			phy-mode = "internal";
+			phy-handle = <&gphy_lan1>;
 		};
 	};
+};
 
+/ {
 	/*
-	 * CBM standard and jumbo buffer pools. The sizes are the vendor's
-	 * (9216 standard segments of 2 KiB, 1024 jumbo segments of 8 KiB).
-	 * no-map: cbm_buf.c owns the only access path and must memremap()
-	 * them. Both pools must fall inside the CBM bus master's window, and
-	 * any board including this file must have a memory node reaching
-	 * 0x25200000.
+	 * The buffer manager's standard and jumbo segment pools. The sizes are
+	 * the vendor's: 9216 standard segments of 2 KiB and 1024 jumbo
+	 * segments of 8 KiB. no-map, because the driver takes the only mapping
+	 * of them itself. Both have to fall inside the manager's bus master
+	 * window, and any board including this file must have a memory node
+	 * reaching 0x25200000.
 	 */
 	reserved-memory {
 		#address-cells = <1>;
@@ -215,7 +231,7 @@ cbm_jbo_pool: cbm-jbo-pool@24a00000 {
  */
 &cbm {
 	memory-region = <&cbm_std_pool>, <&cbm_jbo_pool>;
-	memory-region-names = "cbm-std-pool", "cbm-jbo-pool";
+	memory-region-names = "std-pool", "jumbo-pool";
 	status = "okay";
 };
 
diff --git a/arch/mips/boot/dts/intel-mips/seale_avm_fritz7560.dts b/arch/mips/boot/dts/intel-mips/seale_avm_fritz7560.dts
index 863ce6fa36f8..a57763836c1f 100644
--- a/arch/mips/boot/dts/intel-mips/seale_avm_fritz7560.dts
+++ b/arch/mips/boot/dts/intel-mips/seale_avm_fritz7560.dts
@@ -240,21 +240,6 @@ partition@1500000 {
 	};
 };
 
-/*
- * The four LAN GPHYs this board populates. The reset bit list is not
- * contiguous, so every entry is spelled out, and the status bit each request
- * bit reports through is a different number again on three of the four -- see
- * the map on the reset controller in xrx500.dtsi.
- */
-&gphy_fw {
-	phy_id = <0x02 0x03 0x04 0x05>;
-	resets = <&rcu_reset0 29 28>,
-		 <&rcu_reset0 28 23>,
-		 <&rcu_reset0 26 22>,
-		 <&rcu_reset0 25 25>;
-	reset-names = "phy2", "phy3", "phy4", "phy5";
-};
-
 /*
  * DECT reset, active high. No DECT driver exists in this tree, so the
  * line is hogged and driven asserted at boot to hold the Dialog DECT
diff --git a/arch/mips/boot/dts/intel-mips/seale_avm_grx550.dtsi b/arch/mips/boot/dts/intel-mips/seale_avm_grx550.dtsi
index 9c6ce5add717..c554b2cfb6c8 100644
--- a/arch/mips/boot/dts/intel-mips/seale_avm_grx550.dtsi
+++ b/arch/mips/boot/dts/intel-mips/seale_avm_grx550.dtsi
@@ -72,21 +72,26 @@ ssogpio: ssogpio@16d00000 {
 };
 
 /*
- * The second MDIO bus, on the GSW-R instance, carrying the WAN PHY. Same
- * sub-aperture of the same IP block as the GSW-L bus in seale_avm.dtsi, so
- * the driver reaches it through the identical offset from this switch node's
- * own base.
- *
- * lantiq,gswip-instance = <1> keeps the GSW-L switch-core re-init off this
- * bus. The PHY is at MDIO address 1.
+ * The second switch macro, which is where this die's WAN socket lands. It is
+ * a tree of its own with a conduit of its own: nothing forwards between the
+ * two macros in hardware, so a bridge spanning them would blackhole, and a
+ * conduit carries exactly one CPU port.
  */
 &gswr {
-	gswr_mdio: mdio {
-		compatible = "lantiq,gswip-mdio";
-		lantiq,gswip-instance = <1>;
+	status = "okay";
+
+	/*
+	 * The same sub-aperture of the same block as the LAN macro's bus, so
+	 * the driver reaches it through the identical offset from this node's
+	 * own window.
+	 *
+	 * The address on the bus is 1, which is the number of the register
+	 * slots the external port's PHY address is written into. It has
+	 * nothing to do with the port's own number.
+	 */
+	mdio {
 		#address-cells = <1>;
 		#size-cells = <0>;
-		status = "okay";
 
 		gphy_wan: ethernet-phy@1 {
 			compatible = "ethernet-phy-idd565.a409",
@@ -94,38 +99,32 @@ gphy_wan: ethernet-phy@1 {
 			reg = <0x01>;
 		};
 	};
+
+	ports {
+		port@f {
+			reg = <15>;
+			label = "wan";
+			phy-mode = "internal";
+			phy-handle = <&gphy_wan>;
+		};
+	};
 };
 
-/*
- * All five GPHYs. The reset bit list is not contiguous, so every entry is
- * spelled out; on this die request bit 31 is the fifth PHY's own reset line
- * and not a group master reset, which is how the vendor device tree names it.
- * That PHY reports its status in bit 30, and bit 31 of the status register
- * belongs to the watchdog instead -- the request bit and the status bit are
- * different numbers on four of these five lines, see the map on the reset
- * controller in xrx500.dtsi. A PHY without firmware never answers MDIO and
- * probe is all-or-nothing.
- */
-&gphy_fw {
-	phy_id = <0x02 0x03 0x04 0x05 0x0F>;
-	resets = <&rcu_reset0 29 28>,
-		 <&rcu_reset0 28 23>,
-		 <&rcu_reset0 26 22>,
-		 <&rcu_reset0 25 25>,
-		 <&rcu_reset0 31 30>;
-	reset-names = "phy2", "phy3", "phy4", "phy5", "phyF";
+&conduit_wan {
+	status = "okay";
 };
 
 /*
- * The WAN port, on the second GSWIP instance. Port id 15 is forced by the
- * allocator range, and lantiq,wan selects the PMAC template for it.
+ * The fifth integrated PHY, which carries the WAN socket. On this die request
+ * bit 31 is that core's own reset line and not a group master reset, which is
+ * how the vendor device tree names it. Its status is reported in bit 30, and
+ * bit 31 of the status register belongs to the watchdog instead.
  */
-&intel_xrx500_net {
-	port@4 {
-		reg = <4>;
-		intel,dp-port-id = <15>;
-		lantiq,wan;
-		phy-handle = <&gphy_wan>;
+&gphy_fw {
+	gphy@f9a {
+		reg = <0xf9a>;
+		resets = <&rcu_reset0 31 30>;
+		reset-names = "gphy";
 	};
 };
 
diff --git a/arch/mips/boot/dts/intel-mips/xrx500.dtsi b/arch/mips/boot/dts/intel-mips/xrx500.dtsi
index 162583c5110e..34262d6ac90f 100644
--- a/arch/mips/boot/dts/intel-mips/xrx500.dtsi
+++ b/arch/mips/boot/dts/intel-mips/xrx500.dtsi
@@ -9,17 +9,6 @@ / {
 	#size-cells = <1>;
 	compatible = "intel,xrx500";
 
-	/*
-	 * The driver takes each controller's id from its alias; the numbering
-	 * indexes enum dma_controller.
-	 */
-	aliases {
-		dma1 = &dma1tx;
-		dma2 = &dma1rx;
-		dma3 = &dma2tx;
-		dma4 = &dma2rx;
-	};
-
 	/*
 	 * Two interAptiv cores, two VPEs each. Only cpu0 carries clocks:
 	 * time.c reads the rate from node 0. The vmb-fw-ipi numbers are
@@ -273,33 +262,87 @@ ebu_nand: nand-controller@16f00000 {
 	};
 
 	/*
-	 * GSWIP-3.x register windows: the two switch cores and the shared
-	 * top-level block, given as physical addresses.
+	 * The two GSWIP-3.0 switch macros. One window each covers the switch
+	 * core, the macro's top block and the packet MAC, so the MDIO bus and
+	 * the loader for the integrated PHYs are children with no window of
+	 * their own.
+	 *
+	 * Two trees rather than two switches in one tree: a conduit carries
+	 * exactly one CPU port, and nothing forwards between the macros in
+	 * hardware, so a bridge spanning both would blackhole.
+	 *
+	 * dma-noncoherent is not decoration. The PHY cores fetch their
+	 * firmware without taking part in the platform's cache coherence, so
+	 * the staged image has to be written back before they are released.
 	 */
-	gswl: gswip@1c000000 {
-		compatible = "intel,xrx500-gswip-3.0";
+	gswl: switch@1c000000 {
+		compatible = "lantiq,xrx500-gswip";
 		reg = <0x1c000000 0x4000>;
-		lantiq,gswip-instance = <0>;
+		dsa,member = <0 0>;
+		dma-noncoherent;
 		status = "disabled";
+
+		ports {
+			#address-cells = <1>;
+			#size-cells = <0>;
+
+			/*
+			 * The CPU port. Which conduit it reaches is on the die
+			 * and not on the board: this macro's frames leave over
+			 * the second pair of DMA controllers and no other.
+			 */
+			port@0 {
+				reg = <0>;
+				phy-mode = "internal";
+				ethernet = <&conduit_lan>;
+
+				fixed-link {
+					speed = <1000>;
+					full-duplex;
+				};
+			};
+		};
 	};
 
-	gswr: gswip@1a000000 {
-		compatible = "intel,xrx500-gswip-3.0";
+	gswr: switch@1a000000 {
+		compatible = "lantiq,xrx500-gswip-r";
 		reg = <0x1a000000 0x4000>;
-		lantiq,gswip-instance = <1>;
+		dsa,member = <1 0>;
+		dma-noncoherent;
 		status = "disabled";
+
+		ports {
+			#address-cells = <1>;
+			#size-cells = <0>;
+
+			port@0 {
+				reg = <0>;
+				phy-mode = "internal";
+				ethernet = <&conduit_wan>;
+
+				fixed-link {
+					speed = <1000>;
+					full-duplex;
+				};
+			};
+		};
 	};
 
 	/*
-	 * Central Buffer Manager. The reg-names order must match the driver's
-	 * ioremap table. The interrupts are level-high.
+	 * Packet buffer manager. It owns the segment pools every packet on the
+	 * datapath is held in, the enqueue and dequeue managers, the traffic
+	 * manager between them, and the table binding an endpoint to a queue.
+	 * Both switch macros and all four DMA controllers draw from it.
+	 *
+	 * A conduit is a processor's end of one macro's datapath and owns no
+	 * window of its own, so it is a child of this node. There are two of
+	 * them because a conduit carries exactly one CPU port.
 	 */
-	cbm: cbm@1e700000 {
-		compatible = "lantiq,cbm-xrx500";
+	cbm: ethernet@1e700000 {
+		compatible = "lantiq,xrx500-cbm";
 		reg = <0x1e700000 0x1000>,    /* tmu      */
 		      <0x1e710000 0x10000>,   /* cbm      */
 		      <0x1e720000 0x4000>,    /* qidt     */
-		      <0x1e730000 0x14000>,   /* sbim     */
 		      <0x1e750000 0x400>,     /* qeqcnt   */
 		      <0x1e750800 0x400>,     /* qdqcnt   */
 		      <0x1e760000 0x10000>,   /* ls       */
@@ -307,97 +350,77 @@ cbm: cbm@1e700000 {
 		      <0x1e7c0000 0x30000>,   /* dqm      */
 		      <0x1e800000 0x100000>,  /* fsqm0    */
 		      <0x1e900000 0x100000>,  /* fsqm1    */
-		      <0x1e500000 0x100000>;  /* dma_desc */
-		reg-names = "tmu", "cbm", "qidt", "sbim",
-			    "qeqcnt", "qdqcnt", "ls", "eqm", "dqm",
-			    "fsqm0", "fsqm1", "dma_desc";
+		      <0x1e500000 0x100000>;  /* dma-desc */
+		reg-names = "tmu", "cbm", "qidt", "qeqcnt", "qdqcnt", "ls",
+			    "eqm", "dqm", "fsqm0", "fsqm1", "dma-desc";
 		interrupt-parent = <&gic>;
 		interrupts = <GIC_SHARED 174 IRQ_TYPE_LEVEL_HIGH>,
 			     <GIC_SHARED 178 IRQ_TYPE_LEVEL_HIGH>,
 			     <GIC_SHARED 179 IRQ_TYPE_LEVEL_HIGH>,
 			     <GIC_SHARED 180 IRQ_TYPE_LEVEL_HIGH>,
 			     <GIC_SHARED 181 IRQ_TYPE_LEVEL_HIGH>;
+		interrupt-names = "cbm", "ls0", "ls1", "ls2", "ls3";
 		clocks = <&cgu CLK_CBM>, <&cgu GCLK_CBM>;
 		clock-names = "freq", "cbm";
+		#address-cells = <1>;
+		#size-cells = <0>;
 		status = "disabled";
+
+		conduit_lan: ethernet@0 {
+			compatible = "lantiq,xrx500-conduit";
+			reg = <0>;
+			lantiq,transmit-dma = <&dma2tx>;
+			lantiq,receive-dma = <&dma2rx>;
+		};
+
+		/*
+		 * The second macro carries a front-panel socket only on the
+		 * die variant that wires one, so its conduit is left to the
+		 * board files to enable.
+		 */
+		conduit_wan: ethernet@1 {
+			compatible = "lantiq,xrx500-conduit";
+			reg = <1>;
+			lantiq,transmit-dma = <&dma1tx>;
+			lantiq,receive-dma = <&dma1rx>;
+			status = "disabled";
+		};
 	};
 
-	/* The three GSWIP-3.0 DMA controller register windows. */
+	/*
+	 * The four packet DMA controllers, a transmit and a receive one for
+	 * each switch macro. Their channels are hard-bound to a macro and to a
+	 * port of the buffer manager, so they offer nothing to an arbitrary
+	 * consumer and are reached through the phandle of the endpoint that
+	 * uses them. How each instance arbitrates, bursts, polls and places
+	 * its descriptors is fixed by the die and belongs to the driver.
+	 */
 	dma1tx: dma@1a100000 {
-		compatible = "lantiq,dma-xrx500";
+		compatible = "lantiq,xrx500-dma";
 		reg = <0x1a100000 0x10000>;
 		interrupt-parent = <&gic>;
 		interrupts = <GIC_SHARED 65 IRQ_TYPE_LEVEL_HIGH>;
-		lantiq,dma-pkt-arb = <2>; /* 0 - burst, 1 - mulitburst, 2 - pkt */
-		lantiq,dma-burst = <16>; /* 2, 4, 8, 16 */
-		lantiq,dma-polling-cnt = <108>;
-		lantiq,dma-chan-fc = <1>;
-		lantiq,dma-desc-fod = <1>;
-		lantiq,dma-desc-in-sram = <1>; /* A11 - 0, A21 - 1 */
-		lantiq,dma-drb = <0>;
-		lantiq,dma-byte-en = <1>;
-		lantiq,budget = <20>;
-		lantiq,dma-lab-cnt = <2>; /* 0, 1, 2, 3 */
-		lantiq,dma-txendi = <3>;
-		lantiq,dma-rxendi = <3>;
-		status = "okay";
 	};
 
 	dma1rx: dma@1a200000 {
-		compatible = "lantiq,dma-xrx500";
+		compatible = "lantiq,xrx500-dma";
 		reg = <0x1a200000 0x10000>;
 		interrupt-parent = <&gic>;
 		interrupts = <GIC_SHARED 66 IRQ_TYPE_LEVEL_HIGH>;
-		lantiq,dma-pkt-arb = <2>; /* 0 - burst, 1 - mulitburst, 2 - pkt */
-		lantiq,dma-burst = <16>; /* 2, 4, 8, 16 */
-		lantiq,dma-polling-cnt = <24>;
-		lantiq,dma-chan-fc = <0>;
-		lantiq,dma-desc-fod = <1>;
-		lantiq,dma-desc-in-sram = <0>;
-		lantiq,dma-drb = <0>;
-		lantiq,dma-byte-en = <0>;
-		lantiq,dma-txendi = <3>;
-		lantiq,dma-rxendi = <3>;
-		lantiq,budget = <20>;
-		status = "okay";
 	};
 
 	dma2tx: dma@1c100000 {
-		compatible = "lantiq,dma-xrx500";
+		compatible = "lantiq,xrx500-dma";
 		reg = <0x1c100000 0x10000>;
 		interrupt-parent = <&gic>;
 		interrupts = <GIC_SHARED 67 IRQ_TYPE_LEVEL_HIGH>;
-		lantiq,dma-pkt-arb = <2>; /* 0 - burst, 1 - mulitburst, 2 - pkt */
-		lantiq,dma-burst = <16>; /* 2, 4, 8, 16 */
-		lantiq,dma-polling-cnt = <24>;
-		lantiq,dma-chan-fc = <1>;
-		lantiq,dma-desc-fod = <1>;
-		lantiq,dma-desc-in-sram = <0>;
-		lantiq,dma-drb = <0>;
-		lantiq,dma-byte-en = <1>;
-		lantiq,dma-txendi = <3>;
-		lantiq,dma-rxendi = <3>;
-		lantiq,budget = <20>;
-		status = "okay";
 	};
 
 	dma2rx: dma@1c200000 {
-		compatible = "lantiq,dma-xrx500";
+		compatible = "lantiq,xrx500-dma";
 		reg = <0x1c200000 0x10000>;
 		interrupt-parent = <&gic>;
 		interrupts = <GIC_SHARED 68 IRQ_TYPE_LEVEL_HIGH>;
-		lantiq,dma-pkt-arb = <2>; /* 0 - burst, 1 - mulitburst, 2 - pkt */
-		lantiq,dma-burst = <16>; /* 2, 4, 8, 16 */
-		lantiq,dma-polling-cnt = <24>;
-		lantiq,dma-chan-fc = <0>;
-		lantiq,dma-desc-fod = <1>;
-		lantiq,dma-desc-in-sram = <0>;
-		lantiq,dma-drb = <0>;
-		lantiq,dma-byte-en = <1>;
-		lantiq,dma-txendi = <3>;
-		lantiq,dma-rxendi = <3>;
-		lantiq,budget = <20>;
-		status = "okay";
 	};
 
 	/*
